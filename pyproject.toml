[project]
name = "gpu-scaling-experiments"
version = "0.1.0"
description = "KV-cache capacity and decode-speed studies for Qwen3.6 serving on H200s (vLLM)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "pandas>=2.0",
    "scipy>=1.13",
    "matplotlib>=3.9",
]
